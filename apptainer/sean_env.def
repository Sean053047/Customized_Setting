Bootstrap: docker
From: nvidia/cuda:12.8.0-runtime-ubuntu22.04


%post
	apt-get update && apt-get install -y \
	wget bzip2 htop nvtop zsh ca-certificates git \
	tmux vim sudo net-tools

	mkdir /data
# %apprun shell
# 	exec zsh